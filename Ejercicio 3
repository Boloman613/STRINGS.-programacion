#include<iostream>
#include<string>
using namespace std;

int main(){
   string frase;
   cout<<"ingresa una frase:";
   getline(cin,frase);
   
    if (!frase.empty()) {
        frase[0] = toupper(frase[0]); 
    }

    cout << "frase: " << frase << endl;
    return 0;
}
