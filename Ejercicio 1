#include<iostream>
#include<string>
using namespace std;
int main(){
    string texto;
    cout<<"ingresar texto:";
    getline(cin,texto);

    for(char&c: texto){
        c=toupper(c);
    }
        cout<< "texto en mayuscula:"<<texto<<endl;

        return 0;
        }
