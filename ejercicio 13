#include <iostream>
#include <string>
using namespace std;

int BuscarSubCadena(string sub, string cad) {
    size_t pos = cad.find(sub); 
    if (pos == string::npos)
        return -1;             

    return pos + 1;             

int main() {
    string cadena, subcadena;

    cout << "Cadena: ";
    getline(cin, cadena);

    cout << "Subcadena: ";
    getline(cin, subcadena);

    int
