#include <iostream>
#include <string>
#include <vector>
using namespace std;

bool esVocal(char c) {
    c = tolower(c);
    return (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u');
}

vector<string> vocalesJuntas(const str
